import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.crismonicwave.recipesgalore.mobile',
  appName: 'recipesgalore-mobile',
  webDir: 'www'
};

export default config;
